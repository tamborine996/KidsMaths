# USER.md

This file is now a compatibility pointer only.

For KidsMaths project-level intent, use:
- `KIDSMATHS_PROJECT_INTENT.md`

Why:
- `USER.md` was too easy to misread as the highest-level global file about Mohammed across all projects.
- In this repo, the canonical project-level source of truth should be explicit and project-named.

Use this file only to redirect older references.
Do not add new project guidance here.

For durable global preferences about Mohammed across projects, use Hermes memory / global skills instead of this repo file.
